node_modules/
dist/
build/
.next/
out/
.expo/
*.tsbuildinfo

.env
.env.local
!.env.example

*.log
.DS_Store

# Service account credentials — never commit
*service-account*.json
credentials/

# Personal/local tool permissions, not shared config
.claude/settings.local.json

# Sheets backup exports (apps/api/scripts/backup-sheets.ts) — real donor/institution data, never committed
apps/api/backups/

# Local reference screenshots from browser-preview checks — snapshots for comparison, not project source
reference-screenshots/

# EAS build output artifacts (e.g. from `eas build --local`) — build products, not source
*.ipa
*.apk
*.aab
